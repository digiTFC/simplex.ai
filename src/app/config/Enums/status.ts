export enum UserStatus {
    ADMINISTRATOR,
    CLIENT,
}

export enum ChatBotStatus {
    INACTIF = "INACTIF",
    ACTIF = "ACTIF",
    EXPIRED = "EXPIRED"    
}

export enum ChatBotPlatForm {
    "SITE WEB",
    "LOCAL APP",
}